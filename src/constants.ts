// =========
// CONSTANTS
// =========

/** ID of the extension. Must match the `package.json` */
export const EXTENSION_ID = "bg3-modding";

/** Constants used for internal purposes */
export const INTERNAL = {
    /** The key name of the attributes group to be used with the XML parser */
    attributesGroupName: "_@_"
};
